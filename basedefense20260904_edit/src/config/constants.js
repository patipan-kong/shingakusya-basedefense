// ค่าคงที่สำหรับปรับ Balance ของเกม รวมไว้ที่เดียวเพื่อไม่ต้องไล่หา Magic Number ในไฟล์ Scene

// ----- Dev Tools (ปุ่มเฉพาะฝั่งทดสอบที่หน้า Base/MapScene) -----
// ปิดตัวเหล่านี้ก่อนส่งบิลด์ให้ลูกค้าดู กันลูกค้าเห็น/กดปุ่มที่ไม่เกี่ยวกับ gameplay จริงโดยไม่ตั้งใจ
// ตัวแม่: ปิดตัวนี้ตัวเดียวซ่อนไอคอน gear มุมขวาบนของหน้า Base ทั้งอัน (ทางเข้าเดียวไปแผง DevToolsScene) — ตัวย่อยด้านล่าง
// (SHOW_BOT_BUTTON ฯลฯ) ยังคงมีผลต่อว่าปุ่มไหนโชว์ "ภายใน" แผงนั้นถ้าเปิดตัวแม่ไว้ ปิดตัวแม่ = ไม่ต้องไปปิดตัวย่อยทีละตัวก็ได้
export const SHOW_DEV_TOOLS_GEAR = true;
export const SHOW_BOT_BUTTON = false;         // ปุ่ม "ปล่อยบอทเล่นเอง" (ดู AutoPlayBot.js)
export const SHOW_TEST_MODE_BUTTON = false;   // ปุ่ม "TEST MODE" (เลือก Stage/เลเวลป้อม-ฐานเอง ดู testMode.js)
export const SHOW_QUIZ_PREVIEW_BUTTON = false; // ปุ่ม "QUIZ PREVIEW" (ไล่ดูคำถามทุกข้อตามลำดับ ดู quizPreview.js)
// ปุ่มเร่งความเร็วเกม (2x/5x/10x/20x มุมบนซ้ายของ MapScene) — ปิดได้แค่ตัว UI เฉยๆ ฟังก์ชัน setGameSpeed() ยังทำงานปกติ
// (บอทยังเรียก scene.setGameSpeed(20) ตรงๆ เพื่อเร่งทดสอบได้เหมือนเดิม ดู AutoPlayBot.js)
export const SHOW_SPEED_BUTTONS = false;

// ----- Base (ฐานทัพหลัก) -----
export const BASE_WIDTH = 640;
export const BASE_HEIGHT = 280;
// ดาเมจที่ฐานโดนตอนนี้มาจาก atk ของศัตรูแต่ละชนิด (ดู ENEMY_DAMAGE_PER_ATK_RANK ด้านล่าง) ไม่ใช่ค่าคงที่ตายตัวอีกต่อไป
export const BASE_HIT_SHAKE_DURATION_MS = 150; // จอสั่นตอนศัตรูชนฐานทัพ นานเท่านี้
export const BASE_HIT_SHAKE_INTENSITY = 0.01;  // ความแรงสั่น (สัดส่วนของขนาดจอ ตามหน่วยของ Phaser camera.shake)

// ----- Turrets (ป้อมปืน) -----
export const TURRET_COUNT = 5;
export const TURRET_MARGIN_X = 102;
export const TURRET_Y_OFFSET_FROM_BASE_TOP = 25; // ตำแหน่งป้อมปืนต่ำกว่าขอบบนของฐานทัพเท่าไร
export const TURRET_FIRE_STAGGER_MS = 150; // หน่วงเวลาเริ่มยิงของแต่ละป้อมไม่ให้ยิงพร้อมกันหมด
export const BARREL_LENGTH = 70; // ระยะจากจุดหมุนป้อม (center ของภาพ gun_*.png หลัง setOrigin(0.5,0.5)) ถึงปากกระบอกปืนจริง — ผู้ใช้วัดจากภาพจริงมาให้ (70px)
export const BULLET_SPEED = 500;
export const TURRET_ROTATION_SPEED_RAD_PER_SEC = Math.PI * 4; // ความเร็วหมุนกระบอกปืนตามศัตรู (เรเดียน/วินาที) — หมุนครบรอบใน 0.5 วินาที ยิ่งค่ามากยิ่งหันไวขึ้น
export const TURRET_RANGE_PX = 450; // ระยะยิงสูงสุด (px) — ป้อมจะไม่ยิง/ไม่หมุนเล็งศัตรูที่อยู่ไกลเกินนี้ ทำให้แต่ละป้อมมี "โซนรับผิดชอบ" ของตัวเอง ไม่ใช่ยิงได้ทั่วจอ

// ----- Turret Stat Scaling: แปลงค่า Rank 1-5 ใน src/data/turretTypes.js ให้เป็นหน่วยเกมจริง -----
// ปรับค่าพวกนี้เพื่อรีบาลานซ์ป้อมปืน "ทั้งเกมพร้อมกัน" — ถ้าจะปรับป้อมกระบอกใดกระบอกหนึ่งเป็นการเฉพาะ ให้ไปแก้ที่ turretTypes.js แทน
// เดิม 3 — ทดสอบเล่นจริงแล้วโดยผู้ใช้ที่ค่า 1: ไม่อัพเกรดเลย เคลียร์ Stage 1-2 hp เหลือ 100%, Stage 3 เสีย 54%, ตายที่ Stage 4 Wave 5 — โค้งความยากที่ต้องการพอดี
// (ค่านี้คูณเข้าสูตร DPS แบบเส้นตรง ทำให้ Level 1 อ่อนลงเหลือ 1/3 — ส่วนเพดาน Level 5 ถูกชดเชยคืนแยกต่างหากที่ turretTypes.js เพื่อไม่ให้อ่อนตามไปด้วย)
// 2026-09-01 (รอบ 3): ทั้ง 2000ms และ 1500ms (ดูประวัติด้านล่าง) ยังทำให้ DPS ต้นเกมหายไป 40-58% เกินกว่าที่ Lv.1-2 จะไหว —
// ผู้เล่นจริงเทสต์แล้วติดที่ Stage 4-5 ตายรวด 9 ครั้ง (gold-starvation) เร็วกว่าที่เทสต์อัตโนมัติชี้ไว้อีก (Stage 10) จึงต้อง
// ชดเชยดาเมจ/นัดขึ้นที่นี่ (คูณ K=1.782 ให้ DPS รวม Lv.1 กลับไปที่ ~23.7 เดิมเป๊ะ) พร้อมลด atkGrowthPerLevel ต่อป้อมลง
// (ดู turretTypes.js) ให้เพดาน Lv.5 ยังคงเดิม 177 DPS ไม่เปลี่ยน — ผลข้างเคียงที่ตั้งใจ: Lv.2-4 ได้ DPS มากกว่าเดิมเล็กน้อยด้วย
// (28.1/49.6/91.4 เทียบเดิม 27.0/45.3/84.8) เผื่อพื้นที่ปลอดภัยเพิ่มให้ Stage ที่เคยเฉียดตายในตัวเทสต์รอบก่อน
export const TURRET_DAMAGE_PER_ATK_RANK = 1.782; // atk rank (1-5) x ค่านี้ = ดาเมจต่อการยิงโดน 1 ครั้ง
// 2026-09-01: ลูกค้าคอมเมนต์ Lv.1 ยิงไวเกินไป + อยากให้ทุกป้อมเริ่มเท่ากัน — เดิมค่านี้ผสมกับ type.fireRate rank ทำให้ Lv.1
// ห่างกัน 400-1400ms ตอนนี้ตัด fireRate ออกจากฐานแล้ว (ดู turretStats.js getTurretFireDelay) ค่านี้จึงเป็นหน่วงเวลา Lv.1 ตรงๆ
// ของทุกป้อมเท่ากันหมด — ลองที่ 2000ms (2 วิ) ก่อนแล้วทดสอบจริงพบว่า DPS ต้นเกมหายไปเยอะเกิน (Stage 1 Lv.1 เหลือ HP แค่ 30%,
// Stage 10/11/18/20/21/22 ตายที่เกียร์ที่เคยผ่านได้) จึงลดลงมาที่ 1500ms (1.5 วิ) ตามที่ตกลง — DPS ที่หายไปชดเชยคืนแล้วผ่าน
// TURRET_DAMAGE_PER_ATK_RANK ด้านบนแทน (ไม่ใช่จากตรงนี้)
export const TURRET_FIRE_DELAY_BASE_MS = 1500;
// ----- Turret Spread Shot (ป้อมที่ spread:true ในข้อมูล) -----
// จำนวนนัดต่อการยิง 1 ครั้งตอนนี้ผันตามเลเวล ดูที่ turretTypes.js -> upgrade.bulletCountByLevel แทนค่าคงที่นี้
export const TURRET_SPREAD_ANGLE_DEG = 30; // มุมกระจายรวมทั้งหมด (องศา) รอบทิศทางที่เล็งอยู่

// ----- Turret Evolution (5 ระดับ) -----
// เลเวลจริงอ่านจาก SaveManager (ผู้เล่นอัปเกรดผ่าน UpgradeScene)
// อัตราการโต (ATK/อัตรายิง/จำนวนกระสุน) ตอนนี้ "ต่างกันไปตามป้อม" ดูสูตรที่ src/data/turretTypes.js -> upgrade
// (จงใจไม่ใช้ค่าคงที่กลางค่าเดียวอีกต่อไป เพื่อให้แต่ละป้อมมีเส้นทางอัปเกรดที่ให้ผลต่างกันจริง)
export const TURRET_MAX_LEVEL = 5;
// ปัจจุบันไม่ได้ใช้แล้ว (map.js: updateTurretVisualScale เป็น no-op) — เก็บไว้เผื่อกลับมาใช้ตอนมี spritesheet ป้อมปืนครบทุกเลเวลจริง (ตอนนั้นน่าจะสลับเฟรมแทนขยาย scale)
export const TURRET_VISUAL_SCALE_GROWTH_PER_LEVEL = 0.15;

// ----- Upgrade System (UpgradeScene) -----
// ไม่มีสเปคราคา/ผลตอบแทนจากเอกสารเลย ค่าด้านล่างเป็นค่าเริ่มต้นที่ตั้งเอง ปรับได้อิสระ
// ประวัติ: เดิมราคาป้อม/ฐานคิดจากสูตรทวีคูณคนละชุดกัน (BASE_COST x MULTIPLIER^level ตัวคูณ 1.8->2.2->2.8->1.6->2.3)
// เจตนาตอนนั้นคือเลเวลแรกถูก เลเวลท้ายแพงก้าวกระโดด บังคับไล่เคลียร์หลาย Stage กว่าจะอัปครบ (ไม่ใช่ 1-2 Stage ก็ Max ได้ทุกอย่าง)
// ปรับ 2026-08-27 ตามที่ลูกค้าต้องการ: เปลี่ยนเป็นตารางราคาตายตัวที่กำหนดเอง ให้ตัวเลขสวย/ทำนายง่าย
// และป้อมปืนกับฐานทัพราคาเท่ากันทุกเลเวล (ก่อนหน้านี้ราคาป้อม/ฐานใกล้เคียงกันโดยบังเอิญจากสูตรคนละชุด ไม่ได้ตั้งใจให้เท่ากันเป๊ะ)
// รวมต่อ 1 ช่อง (ป้อม 1 กระบอก หรือ ฐาน 1 อัน) = 150+350+800+1800 = 3,100 — ป้อม 5 กระบอก + ฐาน 1 = รวมทั้งเกม 18,600
export const UPGRADE_COST_BY_LEVEL = [150, 350, 800, 1800]; // ราคาอัปจากเลเวล 1->2, 2->3, 3->4, 4->5 ตามลำดับ ใช้ร่วมกันทั้งป้อมปืนและฐานทัพ

export const BASE_UPGRADE_MAX_LEVEL = 5; // ฐานทัพวิวัฒนาการรูปลักษณ์ได้ 5 ระดับตามดีไซน์ (มากกว่าป้อมปืนที่มี 3)

// ----- Base Passive Skill (แทนที่การอัปเกรดฐานทัพแบบ Max HP บวกตรงๆ เดิม) -----
// แต่ละเลเวลคือ Passive Skill ที่แรงขึ้น: Max HP เพิ่มขึ้น + HP Regen ปลดล็อกตั้งแต่เลเวล 2 แล้วแรงขึ้นเรื่อยๆ
// ปรับ 2026-08-28: กลับมาใช้สูตร backloaded (ไม่ใช่ตารางเลขนิ่งแล้ว) คู่กับป้อมปืน (ดู turretStats.js/baseStats.js) — พบว่าฐานทัพ (ไม่ใช่ดาเมจป้อม)
// คือตัวที่แบกเกมไว้เกือบทั้งหมด เพราะยังเป็นตารางโตช้าๆ สม่ำเสมออยู่ ทำให้แค่มีฐานเลเวลกลางๆ ก็รอดได้แทบทุก Stage — เพดานเลเวลสูงสุดยังคงเดิมเป๊ะ (HP 200, Regen 2.5%/วิ)
export const BASE_MAX_HP = 100;                    // Max HP ที่เลเวล 1 (ฐานอ้างอิงก่อนคูณ multiplier ต่อเลเวล)
export const BASE_HP_MULTIPLIER_PER_LEVEL = 0.25;  // สัดส่วน HP ที่เพิ่มขึ้นที่เลเวลสูงสุด (เทียบเลเวล 1) เช่นเดียวกับก่อนหน้า — เพดานคือ 100 x (1+0.25x4) = 200
export const BASE_HP_REGEN_UNLOCK_LEVEL = 2;       // level 1 ยังไม่มี Regen เลย ต้องอัปอย่างน้อย 1 ครั้งก่อน
export const BASE_HP_REGEN_PERCENT_PER_SEC_BASE = 0.01;      // %maxHP/วิ ที่ปลดล็อกแรก (level2) — ไม่ผันตาม curve เพราะเป็นจุดเริ่มปลดล็อก ไม่ใช่ผลตอบแทนต่อเลเวล
export const BASE_HP_REGEN_PERCENT_PER_SEC_PER_LEVEL = 0.005; // ใช้คำนวณเพดานที่เลเวลสูงสุด (level5 = 2.5%/วิ) เลเวลกลางๆ (3-4) อ่อนกว่านี้มากตามสูตร backloaded
export const BASE_HP_REGEN_TICK_MS = 1000;

// ----- Enemies -----
export const ENEMY_SPAWN_MARGIN_X = 30;
export const ENEMY_HIT_FLASH_DURATION_MS = 100; // ระยะเวลา Flash ตอนศัตรูโดนยิงแต่ยังไม่ตาย
export const ENEMY_HIT_FLASH_SCALE_MULTIPLIER = 1.15; // ขยายขนาดชั่วคราวตอนโดนยิง (คูณกับขนาดจริงของศัตรูตัวนั้น ไม่ใช่ค่าตายตัว)

// ----- Enemy Stat Scaling: แปลงค่า Rank 1-5 ใน src/data/enemyTypes.js ให้เป็นหน่วยเกมจริง -----
// ปรับค่าพวกนี้เพื่อรีบาลานซ์ศัตรู "ทั้งเกมพร้อมกัน" — ถ้าจะปรับศัตรูชนิดใดชนิดหนึ่งเป็นการเฉพาะ ให้ไปแก้ที่ enemyTypes.js แทน
// DPS ป้อมปืนรวมทั้ง 5 กระบอก ณ เลเวล 1 (ยังไม่อัปเกรด) คำนวณจริงจากค่าปัจจุบันได้ราว 70 ต่อวินาที
// ลดจาก 20 -> 8: ทำให้ dur1 (ศัตรูอ่อนสุด เช่น speed1/speed2/normal2) = 8 HP ตายได้ในนัดเดียวจากป้อมส่วนใหญ่ที่เลเวล 1
// (ป้อม spread ยิงหลายนัดแรงน้อยต่อนัด อาจต้อง 2 นัด — ตรงตามคาแรกเตอร์ป้อม spread ที่ออกแบบไว้ ไม่ใช่บั๊ก)
// เกมยาวขึ้นให้อึดขึ้นผ่านตัวคูณตาม Stage ด้านล่าง ไม่ใช่ผ่านค่านี้ตรงๆ อีกต่อไป
export const ENEMY_HP_PER_DURABILITY_RANK = 8;
export const ENEMY_SPEED_PER_RANK = 20;          // moveSpeed rank (1-5) x ค่านี้ = ความเร็วตกจริง (px/s)
export const ENEMY_DAMAGE_PER_ATK_RANK = 5;      // atk rank (1-5) x ค่านี้ = ดาเมจที่ฐานโดนเมื่อศัตรูตัวนี้ชน (หรือดาเมจ/นัด ถ้าเป็นศัตรูระยะไกล — ดูด้านล่าง)
// 2026-08-28: เคยยกกำลัง rank ก่อนคูณ (>1) เพื่อบีบให้ยากขึ้นเองตามธรรมชาติที่ Stage หลังๆ (ใช้ rank สูงเยอะกว่า) — ได้ผลจริงแต่แก้ปัญหาผิดจุด
// (ไล่ตาม "DPS ราบเรียบทั้ง 50 Stage" ซึ่งไม่ตรงเจตนาจริงของลูกค้า) เปลี่ยนแนวทางใหม่: ยึดตารางสิ่งใหม่/จำนวนธาตุต่อ wave ที่ลูกค้าให้มาแทน
// (Stage 1-4/5-8/9-14/15-22/23-30/31-50 ไล่ตามชนิดศัตรู+จำนวนธาตุที่เข้ามาใหม่ ไม่ใช่ DPS) — ความยากที่แท้จริงควรมาจาก "ศัตรูชนิดใหม่ที่แข็งกว่าเข้ามา"
// (ออกแบบไว้ในสเปกอยู่แล้วผ่าน rank ต่อ type ใน enemyTypes.js) และ "ต้องมีป้อมครบธาตุจริง" (โบนัส 2x ตอนธาตุตรง ดู ELEMENT_MATCH_DAMAGE_MULTIPLIER
// ป้อมทั้ง 5 กระบอกมีธาตุคงที่ครบทั้ง 4 ธาตุอยู่แล้วโดยดีไซน์) ไม่ต้องยกกำลัง rank ศัตรูเพิ่มอีกชั้น — กลับไปใช้เส้นตรงเดิม (power=1 = ไม่มีผล)
export const ENEMY_RANK_CURVE_POWER = 1;

// ----- ศัตรูระยะไกล (type.isRanged: ranged/mediumRanged) — ไม่เดินเข้าชนฐานแบบศัตรูทั่วไป แต่หยุดยืนยิงฐานจากระยะแทน -----
// ดาเมจ/นัด = atk x ENEMY_DAMAGE_PER_ATK_RANK x ตัวคูณ Stage x ENEMY_RANGED_DAMAGE_MULTIPLIER (ตัวคูณลดสัดส่วนต่อนัด เพราะยิงได้ซ้ำหลายนัด ไม่ใช่ครั้งเดียวจบแบบชนฐาน)
// รอบแรก (มัลติพลายเออร์=1, ยิงทุก 1200ms) ผู้ทดสอบจริงพบว่า "โหด รัวๆ เกินไปกว่าจะโดนกำจัด" จึงปรับลดทั้งอัตรายิง+ดาเมจ/นัดพร้อมกันรอบนี้ — ยังไม่มีสเปกสุดท้าย รอปรับต่อจากผลเล่นจริงรอบใหม่
export const ENEMY_RANGED_ATTACK_RANGE_PX = 260; // ระยะ (px เหนือขอบบนฐานทัพ) ที่จะหยุดเดินแล้วเริ่มยิง — เผื่อให้ยังอยู่ในระยะป้อมปืนอย่างน้อย 1-2 ป้อมเสมอ (TURRET_RANGE_PX=450)
export const ENEMY_RANGED_FIRE_INTERVAL_MS = 2200; // หยุดแล้วยิง 1 นัดทุกเท่านี้ (นัดแรกดีเลย์เท่ากันนี้ก่อน ไม่ใช่ยิงทันทีที่หยุด) — เดิม 1200ms เร็วเกินไป
export const ENEMY_RANGED_DAMAGE_MULTIPLIER = 0.6; // ลดดาเมจ/นัดเหลือ 60% ของสูตรชนฐานเดิม กันสะสมดาเมจเร็วเกินไปตอนโดนยิงหลายนัดติดกัน
export const ENEMY_RANGED_PROJECTILE_SPEED = 260; // px/s — ช้ากว่ากระสุนป้อม (BULLET_SPEED=500) ให้ดูเหมือนยิงโค้งจากระยะ ไม่ใช่ยิงตรงเร็วแบบป้อม

// ----- Element System -----
export const ELEMENT_MATCH_DAMAGE_MULTIPLIER = 2; // ดาเมจโบนัสเมื่อยิงศัตรูด้วยธาตุตรงกับตัวมันเอง (จะใช้งานจริงตอนป้อมปืนมีธาตุ)

// ----- Wave / Stage -----
// แคมเปญคือ 50 Stage x 10 Wave — กำหนดศัตรูทุก Wave ไว้ตายตัวตามสเปกลูกค้า (ดู src/data/waveSpec.js/stages.js)
export const WAVES_PER_STAGE = 10;
export const WAVE_COMPLETE_DELAY_MS = 3000;

// ----- Skill System -----
// ปรับตามที่ลูกค้าแจ้ง (2026-08-26): ใช้ซ้ำได้ไม่จำกัดจำนวนครั้งแล้ว (เดิมแต่ละชนิดใช้ได้แค่ครั้งเดียวต่อการเล่น 1 รอบ)
// แต่ละครั้งที่ร่ายหักเกจตายตัว 1 ขั้นเสมอ (เดิมเลือกหักได้ 1-3 ขั้นตามเลเวลที่เลือก) — เกจสะสมจากการฆ่าศัตรู สูงสุด 3 ขั้นเท่าเดิม
// เลเวลของสกิลตอนนี้ล็อกไว้ที่ 1 เสมอ (ดู MapScene.getSkillLevel()) — ของเดิมเลือกเลเวล 1-3 ได้ตามเกจที่มี ไว้เผื่ออนาคตจะผูกเลเวลกับการอัปเกรดฐานแทน
export const SKILL_MAX_CHARGE = 3; // สะสมชาร์จได้สูงสุด 3 ขั้น
// ผลทดสอบจริงหลังเปลี่ยนเป็นใช้ซ้ำได้ไม่จำกัด (2026-08-26): เกจเต็มเร็วเกินไป ปรับลดอัตราเติมลง 50% (เพิ่มจำนวนตัวที่ต้องฆ่าเป็น 2 เท่า)
export const SKILL_KILLS_PER_CHARGE = 40; // ต้องฆ่าศัตรูกี่ตัวถึงจะได้เกจ +1 ขั้น

// ระเบิดเพลิง (Incendiary Bomb): ทิ้งกลางจอเสมอ (ไม่เล็งเป้า) ดาเมจเป็นวงกลม + ทิ้งเปลวไฟดาเมจต่อเนื่อง เลเวลสูง = รัศมีกว้างขึ้น
export const SKILL_BOMB_BASE_RADIUS = 70;
export const SKILL_BOMB_RADIUS_PER_LEVEL = 35;      // เลเวลยิ่งสูง อาณาเขตยิ่งกว้างขึ้นชัดเจน (lv1=105, lv3=175)
export const SKILL_BOMB_BASE_DAMAGE = 40;           // ดาเมจตอนระเบิดลง (ครั้งเดียว)
export const SKILL_BOMB_DAMAGE_PER_LEVEL = 25;      // lv1=65, lv2=90, lv3=115 — พอฆ่าศัตรูส่วนใหญ่ในระเบิดได้ทันที
export const SKILL_BOMB_BURST_VISUAL_MS = 300;
export const SKILL_BOMB_FIRE_TICK_DAMAGE = 6;       // ดาเมจต่อ tick ของเปลวไฟที่ค้างอยู่ (เลเวล 0 / ฐาน)
export const SKILL_BOMB_FIRE_TICK_DAMAGE_PER_LEVEL = 3; // เลเวลสูง = เปลวไฟยิ่งแรงขึ้นด้วย ไม่ใช่แค่กว้างขึ้นอย่างเดียว (lv1=9, lv2=12, lv3=15 ต่อ tick)
export const SKILL_BOMB_FIRE_TICK_MS = 500;
export const SKILL_BOMB_FIRE_DURATION_MS = 3000;    // เปลวไฟค้าง 3 วิ

// ปืนแสงเลเซอร์ (Laser Cannon): ยิงคงที่ 2 เส้นเสมอ ที่ตำแหน่ง x = 25% และ 75% ของความกว้างจอ (ดู getLaserPositions ใน map.js)
// ปรับ 2026-08-27 ตามที่ลูกค้าต้องการให้ทั้ง 3 สกิลทำดาเมจพอๆ กัน แล้วให้เลือกใช้ตามพื้นที่แทน — ผ่าน 2 รอบการปรับ:
// รอบ 1: ตั้งดาเมจ 120 (=รวมดาเมจสูงสุดตามทฤษฎีของระเบิด) แล้วลดเหลือ 65 (=เท่าดาเมจ burst ของระเบิด) แต่ลูกค้าชี้ว่ายังฆ่าทันทีเหมือนเดิม
// เพราะศัตรูทุกตัวตอนนี้ HP สูงสุดแค่ 40 ตัวเลขดาเมจจึงไม่ใช่ตัวแปรที่ทำให้ได้เปรียบเลย
// รอบ 2 (สาเหตุจริง): เลเซอร์เช็คแค่ระยะ X ไม่จำกัดระยะ Y เลย (damageEnemiesInVerticalBand ใน map.js) จึงกวาดฆ่าทุกตัวในเลนไม่ว่าอยู่สูงต่ำแค่ไหน
// พื้นที่โดนจริง (2 เส้น x กว้าง 100px x สูงเต็มจอ ~168,000 px²) ใหญ่กว่าวงระเบิด (รัศมี 105px ~35,000 px²) ถึง ~4.8 เท่า
// ลูกค้าเลือกคงความสูงเต็มจอไว้ (จุดเด่นของเลเซอร์ที่ต้องมี) แต่ให้ลดดาเมจลงอีกมากเพื่อชดเชยพื้นที่ที่ใหญ่กว่า — หารดาเมจ burst เดิม (65) ด้วยสัดส่วนพื้นที่ (~4.8) แล้วปัดเป็นเลขกลม
// ผลคือเลเซอร์ไม่ฆ่าทุกตัวในนัดเดียวอีกต่อไป (พอกับศัตรูอึด durability 1 อย่างเดียว) กลายเป็นสกิล "โดนกว้างแต่เบา" ต้องอาศัยป้อมปืน/แคสซ้ำช่วยจบ ต่างจากระเบิด/พลาสม่าที่ "โดนแคบแต่หนัก" จบในจุดนั้นได้เลย
export const SKILL_LASER_WIDTH = 50; // ความกว้างครึ่งหนึ่งของลำแสง (px) — ลดลง 30% จาก 96 ตามที่แจ้ง (2026-08-27)
export const SKILL_LASER_VISUAL_MS = 500; // เวลาที่เอฟเฟกต์แสงค้างจอ (เพิ่มจากเดิมให้เห็นชัดขึ้น)
export const SKILL_LASER_BASE_DAMAGE = 10;      // ดาเมจตอนยิงโดน (ครั้งเดียว)
export const SKILL_LASER_DAMAGE_PER_LEVEL = 5;  // lv1=15, lv2=20, lv3=25

// พลาสม่าดีเฟนเดอร์ (Plasma Defender): กางรั้วค้างไว้ 30 วิ ทำดาเมจต่อเนื่องตลอดช่วงเวลานั้น (ไม่ใช่ดาเมจครั้งเดียวจบแบบเดิม) เลเวลสูง = จำนวนแนวรั้วเพิ่ม
export const SKILL_PLASMA_WIDTH = 20; // ความสูงครึ่งหนึ่งของรั้วแต่ละแนว (px) — กว้างขึ้นจากเดิม (14) กันศัตรูเร็วหลุดผ่านไม่โดน tick เลย
export const SKILL_PLASMA_DURATION_MS = 8000; // รั้วค้างอยู่ 8 วินาที
// tick ถี่ขึ้นจากเดิม (400ms) เพราะศัตรูเร็วสุด (100px/s) ข้ามแนวกว้าง 40px ได้ใน 400ms พอดี
// เสี่ยงข้ามไปโดยไม่โดน tick ไหนเลย — ลดเหลือ 100ms เพื่อให้การันตีว่าโดนอย่างน้อย 2-4 ครั้งเสมอไม่ว่าเร็วแค่ไหน
export const SKILL_PLASMA_TICK_MS = 100;
// ปรับ 2026-08-27 คู่กับเลเซอร์/ระเบิด: อิงจากศัตรูความเร็วเฉลี่ย (~60px/s) ข้ามแนวรั้วสูง 40px ใน ~7 tick
// -> ตั้ง tick damage ให้รวมได้ ~120 ในการข้าม 1 รอบของศัตรูความเร็วปานกลาง (ศัตรูช้าโดนสะสมมากกว่า/ศัตรูเร็วโดนน้อยกว่า — เป็นจุดต่างที่ตั้งใจ)
export const SKILL_PLASMA_TICK_DAMAGE_BASE = 10;      // lv1=17, lv2=24, lv3=31 ต่อ tick
export const SKILL_PLASMA_TICK_DAMAGE_PER_LEVEL = 7;

// ----- Economy -----
// เงินรางวัลได้ทั้งตอนแพ้และชนะ = (ผลรวม coinValue ของศัตรูแต่ละชนิด x จำนวนที่กำจัดได้จริง) + โบนัสตามความเสียหายของฐาน
// coinValue ต่อชนิดกำหนดไว้ที่ src/data/enemyTypes.js (ตัวเลขคิดเองยังไม่มีสเปก รอปรับจากการเล่นจริง)
export const REWARD_BASE_DAMAGE_BONUS_MAX = 100; // โบนัสสูงสุดถ้าฐานไม่โดนดาเมจเลยตลอดเกม ลดลงเป็นเส้นตรงตาม % ความเสียหายที่ฐานได้รับ (ฐานเสียหาย 100% = ไม่ได้โบนัสเลย) — ลดจาก 200 ตามสัดส่วนที่ coinValue ลดลง

// ตัวคูณรายได้รวมทั้งระบบ (คูณท้ายสุดใน MapScene.computeReward()) — จุดปรับจูนเศรษฐกิจจุดเดียวของเกม
// เหตุผล: จากผลเทสจริง เล่นสะอาดเคลียร์ครบ 10 ด่านโดยไม่ตายเลยได้เงิน 18,569 = 1.84 เท่าของราคาอัปเกรดทั้งเกม (10,079)
// แปลว่าเส้นโค้งรายได้พื้นฐานสูงเกินไปเอง ไม่ใช่ปัญหาการฟาร์ม/เล่นซ้ำ (ต้นตอ: จำนวนศัตรูต่อด่านโตแบบ quadratic ตาม stages.js)
// ที่ 0.3: เล่นสะอาด = ~5,571 (ยังไม่พออัพครบ ต้องเล่นเก็บบ้าง), เส้นทางจริงแบบที่ผู้ใช้เทส = ~11,600 ≈ 1.15 เท่า -> อัพครบพอดีช่วงจบเกม
// ปรับจูนซ้ำได้ที่ค่านี้ค่าเดียว ไม่ต้องไปไล่แก้ coinValue รายชนิดใน enemyTypes.js
export const REWARD_MULTIPLIER = 0.3;

// ----- Cleanup (ทำลายกระสุน/ศัตรูที่หลุดขอบจอ) -----
export const BULLET_CLEANUP_Y = -20;
export const ENEMY_CLEANUP_Y = 870;

// ----- Depth / Layering (ลำดับการวาดซ้อนทับใน MapScene) -----
// ศัตรู sort ตาม y จริงของตัวเอง (ดู updateEnemyDepth() ใน map.js) ไม่ใช้ค่าคงที่ตายตัว — ยิ่ง y มาก (ใกล้ฐานทัพ) ยิ่งอยู่บนสุด จำลองมุมมองความลึก
export const DEPTH_BACKGROUND = -1; // ภาพพื้นหลัง (gameplay_bg) ต้องอยู่หลังสุดเสมอ ต่ำกว่าทุกอย่างรวมถึงศัตรู/กระสุนที่ depth ปริยาย 0
export const DEPTH_BASE_DECOR = 500; // ของตกแต่งบนฐานทัพ (research_center) อยู่หน้าพื้นหลัง/ศัตรู แต่หลังป้อมปืน+ฐานทัพ+UI เสมอ
export const DEPTH_ENTITY_UI = 1000; // ป้อมปืน + ฐานทัพ + UI ทั่วไป อยู่บนสุดเสมอ สูงกว่าศัตรู/กระสุน/เอฟเฟกต์สกิลทุกกรณี (ศัตรู depth สูงสุดคือ ENEMY_CLEANUP_Y ~870 ยังน้อยกว่านี้)
export const DEPTH_OVERLAY = 2000; // popup Pause / จบเกม ต้องอยู่บนสุดยิ่งกว่า UI ปกติอีกที เพราะต้องคลุมทุกอย่างตอน pause/จบเกม

// ----- สีของ Placeholder Graphics -----
export const COLOR_BASE = 0xbdbdbd;
// ไม่มี COLOR_ENEMY / COLOR_TURRET / COLOR_BULLET คงที่แล้ว — texture พวกนี้วาดเป็นสีขาวล้วนแล้วย้อมสีจริงด้วย setTint()
// ตามข้อมูลธาตุ/ป้อมปืน (ดู src/data/elements.js, src/data/turretTypes.js) ต้องเป็นสีขาวเท่านั้นเพื่อให้ setTint() ได้สีตรงเป๊ะ
// (Phaser คูณสี texture กับสี tint เข้าด้วยกัน ถ้า texture ไม่ใช่ขาวสีที่ได้จะเพี้ยนจากที่ตั้งใจ)
export const COLOR_ENEMY_HIT_FLASH = 0xffffff;

// ----- Font -----
// โหลดจริงผ่าน rexwebfontloaderplugin ใน BootScene.preload() (อ้าง @font-face จาก css/fonts.css)
// ก่อน Scene อื่นจะเริ่มสร้างข้อความใดๆ — ทุก add.text() ในเกมต้องใส่ fontFamily: FONT_FAMILY นี้
export const FONT_FAMILY = 'YasashisaGothicBold';
